"""
config/ — Centralised configuration package.
Use direct imports in your code:
    from config import settings
    from config.settings import validate_config
"""
# No eager imports — avoids circular import issues